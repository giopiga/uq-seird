--- SEIRDmodel folder ---
In this folder all the files concerning the model.

In particular:
> SEIRDmodel.m: contains the model definition (i.e.: the ODE system)
> SEIRDsolver.m: contains the solver, which calling SEIRDmodel.m returns
                    the numerical solution